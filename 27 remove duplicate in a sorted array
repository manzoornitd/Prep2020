int main() {
	int i,n,j;
	cin>>n;
	int a[n];
	for(i=0;i<n;i++)
	cin>>a[i];
	i=0;
	j=1;
	while(j<n)
	{
	     if(a[i]==a[j])
	     j++;
	     else
	     {
	          a[++i]=a[j++];
	     }
	}
	for(j=0;j<=i;j++)
	cout<<a[j]<<" ";
	return 0;
}
