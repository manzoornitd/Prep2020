#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
	int i,n,k,x;
	cin>>n;
	int a[n];
	for(i=0;i<n;i++)
	cin>>a[i];
	set <int> st;
	cin>>k;
	for(i=0;i<n;i++)
	{
	     x=k^a[i];
	     if(st.find(x)!=st.end())
	     cout<<x<<" "<<a[i]<<" ";
	     else
	     st.insert(a[i]);
	}
	return 0;
}
